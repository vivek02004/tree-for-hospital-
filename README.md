# tree-for-hospital-
one platform that models institutions (school/college/hospital) as nodes in a tree. Each node can have departments/units, users (students/patients, staff), resources (classes/appointments), and shared policies. Child nodes inherit or override parent settings.
